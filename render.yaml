services:
  - type: web
    name: edunexus-backend
    runtime: docker
    rootDir: backend
    plan: free
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: CORS_ORIGINS
        sync: false
